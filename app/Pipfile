[[source]]
name = "pypi"
url = "https://pypi.org/simple"
verify_ssl = true

[packages]
pyhumps = ">=1.3.*"
fastapi = ">=0.81.*"
uvicorn = ">=0.18.*"
pytest = ">=6.2.*"
SQLAlchemy = ">=1.4.*"
psycopg2-binary = ">=2.9.*"

[requires]
python_version = "3"