import React from 'react';
import MyButton from "./ui/buttons/MyButton";

const ClipForm = () => {

    return (
        <form>
            <MyButton>Not Valid</MyButton>
        </form>
    );
};

export default ClipForm;